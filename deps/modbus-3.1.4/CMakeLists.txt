CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(TDengine)

LIST(APPEND SRC log.c)
LIST(APPEND SRC modbus.c)
LIST(APPEND SRC modbus-data.c)
LIST(APPEND SRC modbus-rtu.c)
LIST(APPEND SRC modbus-tcp.c)

INCLUDE_DIRECTORIES(.)
ADD_LIBRARY(modbus ${SRC})
